{
  "name": "bambooit",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "scripts": {
    "dev:api": "npm run dev --workspace=backend",
    "dev:web": "npm run dev --workspace=web",
    "dev": "concurrently -n api,web -c green,blue \"npm run dev:api\" \"npm run dev:web\"",
    "typecheck": "npm run generate --workspace=database && npm run build --workspace=database && npm run build --workspace=backend && npm run type-check --workspace=web",
    "build": "npm run generate --workspace=database && npm run build --workspace=database && npm run build --workspace=backend && npm run build --workspace=web",
    "test:smoke:e2e": "npx playwright test --config=apps/web/playwright.config.ts",
    "test:env:up": "docker compose -f docker-compose.test.yml up -d",
    "test:env:down": "docker compose -f docker-compose.test.yml down -v",
    "db:backup": "npx ts-node --esm scripts/backup-database.ts",
    "db:restore": "npx ts-node --esm scripts/restore-database.ts"
  }
}
